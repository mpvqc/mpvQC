# SPDX-FileCopyrightText: mpvQC developers
#
# SPDX-License-Identifier: MIT

name: Install libmpv
description: Installs libmpv - via apt on Linux, or by downloading and extracting on Windows

inputs:
  url:
    description: URL to download libmpv from (Windows only)
    default: https://github.com/shinchiro/mpv-winbuild-cmake/releases/download/20260407/mpv-dev-x86_64-20260407-git-e2180e5.7z
  sha256:
    description: Expected SHA256 checksum (Windows only)
    default: f9094001d74bc883cab2e2ef2eada6cdcb33dbef3d571ead020fd0efc7f7cec9

runs:
  using: composite
  steps:
    - name: Install libmpv
      shell: bash
      run: |
        if [[ "$RUNNER_OS" == "Linux" ]]; then
          sudo apt update -y
          sudo apt install -y libegl1 libmpv2
        elif [[ "$RUNNER_OS" == "Windows" ]]; then
          ARTIFACT="$(basename "${{ inputs.url }}")"
          curl -L -O "${{ inputs.url }}"
          echo -n "${{ inputs.sha256 }} $ARTIFACT" > "$ARTIFACT.sha256"
          sha256sum --check "$ARTIFACT.sha256"
          7z x "$ARTIFACT"
        else
          echo "Unsupported runner OS: $RUNNER_OS" >&2
          exit 1
        fi
